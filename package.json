{
  "name": "snake-rust-wasm",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "wasm": "wasm-pack build ./rust_lib --target web",
    "dev": "vite --port 3000",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "rust_lib": "./rust_lib/pkg",
    "typescript": "^4.9.3",
    "vite": "^4.0.0",
    "vite-plugin-wasm-pack": "^0.1.12"
  },
  "engines": {
    "node": ">=14"
  }
}
